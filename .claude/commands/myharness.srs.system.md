Generate system-wide SRS (Full Module Requirements Definition). Use when: generate full SRS, extract all requirements from spec, create docs/output/srs-systems folder, generate wireframe/ERD, full system SRS, all-module SRS, requirements extraction, requirements definition from product requirements, wireframe, ERD.

Use the agent: myharness.srs.system

Optional: path to input file/folder and scope constraints (default: extract ALL modules from spec): $ARGUMENTS
