# /caveman-commit

Generate an ultra-compressed conventional commit message for staged changes.

## Usage

```
/caveman-commit
```

Reads git diff --staged, outputs a ready-to-paste commit message in Conventional Commits format (≤50 chars subject line).

---

Read `.claude/skills/caveman-commit/SKILL.md` then generate the commit message.
