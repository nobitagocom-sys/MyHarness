# Technology Stack Reference

Full stack is defined in `{{PROJECT_ROOT}}/docs/technical_architecture.md` — Section I.

**One exception pinned here:**

- Tailwind CSS is locked at `^3` — v4 breaks `tailwind.config.ts` and PostCSS setup entirely.

All other packages use `latest` resolved at init time by `init-packages.sh`.
